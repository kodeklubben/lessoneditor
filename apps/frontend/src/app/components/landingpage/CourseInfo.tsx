import "./courseinfo.scss";

type propTypes = {
  lessonTitle: string;
  courseTitle: string;
};

const CourseInfo = ({ lessonTitle, courseTitle }: propTypes) => {
  return <div>hello</div>;
};

export default CourseInfo;
